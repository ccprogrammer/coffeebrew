class CartModel {
  String id, imageUrl, coffee, name, price;
  int item;
  CartModel({
    required this.id,
    required this.imageUrl,
    required this.coffee,
    required this.name,
    required this.price,
    required this.item,
  });
}

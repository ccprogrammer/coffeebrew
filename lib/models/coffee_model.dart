class CoffeeModel {
  String id, imageUrl, coffee, name, description, price;
  int score;

  CoffeeModel({
    required this.id,
    required this.imageUrl,
    required this.coffee,
    required this.name,
    required this.description,
    required this.price,
    required this.score,

  });
}
